# Database Configuration
DB_HOST=mysql          # Use 'mysql' for Docker, 'localhost' for local execution
DB_PORT=3306           # Internal MySQL port (3307 is the mapped port for local access)
DB_USER=your_user      # Replace with your MySQL user
DB_PASSWORD=your_pass  # Replace with your MySQL password
DB_NAME=ai_agents      # Database name

# Application
PORT=3000              # API port